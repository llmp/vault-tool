# The path to the (optional) KeyPass install folder
# setting this parameter will enable the option to use the keepass when prompted for the vault tokens
kps_path: '\path'
kps_writeback_group: 'History_Group'
kps_writeback_history_groups: ['Vault - DESENV']
